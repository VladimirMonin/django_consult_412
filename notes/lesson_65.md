# Lesson 65. Восстановление пароля через email

## Маршруты для сброса пароля 🛣️

Восстановление доступа к аккаунту является критически важной функцией для любого веб-приложения. В Django этот процесс реализуется с использованием встроенных представлений (views) и форм, которые обеспечивают безопасный и управляемый механизм сброса пароля через электронную почту. Этот раздел подробно описывает маршруты и компоненты, задействованные в процессе восстановления пароля.

Процесс восстановления пароля в Django включает в себя несколько последовательных шагов, каждый из которых обрабатывается отдельным URL-маршрутом и соответствующим представлением. Это обеспечивает модульность и гибкость, позволяя разработчикам настраивать каждый этап под свои нужды, от формы запроса на сброс до страницы подтверждения успешной смены пароля.

### Маршрут авторизации users/login/ 🔑

Форма для входа на сайт. Если пользователь забыл свой пароль, ему предоставляется возможность восстановить его через электронную почту, нажав на соответствующую ссылку "Забыли пароль?". Этот маршрут является отправной точкой для всего процесса восстановления доступа.

Основными компонентами, задействованными в маршруте авторизации, являются: URL-путь `users/login/`, класс `LoginView` (или его кастомная реализация, такая как `UserLoginView`), форма `AuthenticationForm` для ввода учетных данных и шаблон `login.html`, который отображает интерфейс входа. В шаблоне `login.html` добавлена прямая ссылка на маршрут сброса пароля, что обеспечивает удобный переход для пользователей.

`users/login/` - страница входа
`LoginView` - класс, который обрабатывает запросы на вход (служебный)
`AuthenticationForm` - форма для входа
`login.html` - шаблон для страницы входа
`UserLoginView` - класс, который обрабатывает запросы на вход наследуется от `LoginView`

Добавили ссылку на восстановление пароля в шаблоне `login.html`, которая ведет на соответствующий URL-адрес:

```html
<p class="text-center">
    Забыли пароль? <a href="{% url 'users:password_reset' %}">Восстановить</a>
</p>
```

### Маршрут для сброса пароля `users/password-reset/` 📧

Этот маршрут предназначен для инициации процесса сброса пароля. Пользователю предлагается ввести свой адрес электронной почты, на который будет отправлена ссылка для дальнейших действий. Это первый шаг в цепочке восстановления доступа, где система проверяет наличие пользователя с указанным email и подготавливает отправку письма.

Ключевые элементы этого маршрута включают URL-путь `users/password-reset/`, служебный класс `PasswordResetView` (или его кастомную версию, например, `CustomPasswordResetView`), форму `PasswordResetForm` (или `CustomPasswordResetForm`) для ввода email, и шаблон `password_reset_form.html` для отображения формы. Имя маршрута `name='password_reset'` используется для удобной ссылки в шаблонах.

Форма для сброса пароля. Надо ввести email, на который будет отправлена ссылка для сброса пароля.

`users/password-reset/` - страница сброса пароля
`PasswordResetView` - класс, который обрабатывает запросы на сброс пароля (сллужебный)
`PasswordResetForm` - форма для сброса пароля (сллужебный)
`password_reset_form.html` - шаблон для страницы сброса пароля
`name='password_reset'` - имя маршрута для сброса пароля
`CustomPasswordResetView` - класс, который обрабатывает запросы на сброс пароля наследуется от `PasswordResetView`
`CustomPasswordResetForm` - форма для сброса пароля наследуется от `PasswordResetForm`

```python
class CustomPasswordResetForm(PasswordResetForm):
    """Кастомная форма для сброса пароля."""
    def __init__(self, *args, **kwargs):
        """Инициализация формы сброса пароля: настройка полей."""
        super().__init__(*args, **kwargs)
        # Кастомизация поля email
        self.fields['email'].widget.attrs.update({
            'class': 'form-control mb-2',
            'placeholder': 'Email'
        })
```

### Маршрут для сброса пароля по ссылке `password-reset/done/` ✅

После того как пользователь отправил форму сброса пароля, система выполняет два ключевых действия: на фоне отправляется электронное письмо со ссылкой для сброса пароля, а пользователю отображается страница с подтверждением того, что письмо было успешно отправлено. Это промежуточный этап, который информирует пользователя о следующем шаге.

Для этого этапа используются шаблон `password_reset_email.html` для формирования содержимого письма (указывается в `CustomPasswordResetView`) и шаблон `password_reset_done.html` для страницы подтверждения. Класс `CustomPasswordResetDoneView`, наследующийся от `PasswordResetDoneView`, обрабатывает логику отображения этой страницы.

На этом этапе происходит 2 действия. Фоном отравляется письмо с ссылкой на сброс пароля. А пользователю показывается страница с сообщением об отправке письма.

`password_reset_email.html` - шаблон для письма с ссылкой на сброс пароля (Указывается в `CustomPasswordResetView`)
`password_reset_done.html` - шаблон для страницы с сообщением об отправке письма
`CustomPasswordResetDoneView` - класс, который обрабатывает запросы на сброс пароля наследуется от `PasswordResetDoneView`

Дополнительно, на странице `password-reset/done/` может быть предоставлена информация о том, что делать, если письмо не пришло, например, проверить папку "Спам" или повторно отправить запрос. Это улучшает пользовательский опыт и снижает количество обращений в поддержку.

Важно отметить, что ссылка для сброса пароля в письме формируется вручную в шаблоне `password_reset_email.html` с использованием переменных контекста `protocol`, `domain`, `uid` и `token`. Django не предоставляет эту ссылку автоматически в готовом виде, поэтому ее необходимо явно конструировать:

```html
{{ protocol }}://{{ domain }}{% url 'users:password_reset_confirm' uidb64=uid token=token %}
```

```python
class CustomPasswordResetDoneView(PasswordResetDoneView):
    """Кастомная реализация подтверждения сброса пароля."""
    template_name = 'users/password_reset_done.html'
```

### Маршрут по которому пользователь переходит по ссылке из письма `reset/<uidb64>/<token>/` 🔗

Этот маршрут является ключевым для завершения процесса сброса пароля. Пользователь переходит по уникальной ссылке, полученной в письме, которая содержит два важных параметра: `uidb64` и `token`. На этой странице отображается форма для ввода нового пароля.

Основные компоненты этого маршрута включают URL-путь `users/reset/<uidb64>/<token>/`, шаблон `password_reset_confirm.html` с формой для сброса пароля, класс `CustomPasswordResetConfirmView` (наследующийся от `PasswordResetConfirmView`) и форму `CustomSetPasswordForm` (наследующуюся от `SetPasswordForm`), которая позволяет пользователю установить новый пароль.

`users/reset/<uidb64>/<token>/` - страница сброса пароля содержит форму для сброса пароля
`password_reset_confirm.html` - шаблон для страницы сброса пароля содержит форму для сброса пароля
`CustomPasswordResetConfirmView` - класс, который обрабатывает запросы на сброс пароля наследуется от `PasswordResetConfirmView`
`CustomSetPasswordForm` - форма для сброса пароля наследуется от `SetPasswordForm`

`uidb64` и `token`: Параметры для безопасного сброса пароля. `uidb64` - закодированный в Base64 идентификатор пользователя, скрывающий реальный ID. `token` - временный криптографический токен, генерируемый системой для подтверждения запроса. Вместе они обеспечивают однократную, ограниченную по времени возможность сброса пароля.

Django не хранит токены сброса пароля в явной модели `PasswordResetToken`. Вместо этого, токены генерируются "на лету" с использованием класса `django.contrib.auth.tokens.PasswordResetTokenGenerator`. Этот механизм основан на криптографических функциях и включает в себя данные пользователя (например, его ID, время последней смены пароля) и секретный ключ Django.

### Сравнение смены и восстановления пароля 🔄

Важно различать два сценария изменения пароля в Django: восстановление пароля (когда пользователь забыл его) и смена пароля (когда пользователь хочет изменить текущий пароль, находясь в личном кабинете). Несмотря на схожесть задачи, они используют разные представления и формы.

**Восстановление пароля (забыл пароль):**
Процесс начинается с `PasswordResetView`, который инициирует отправку письма. После перехода по ссылке из письма, пользователь попадает на `PasswordResetConfirmView`, где используется `SetPasswordForm`. Эта форма требует ввода нового пароля дважды, без необходимости указывать старый пароль.

**Смена пароля в личном кабинете:**
Для смены пароля в личном кабинете используется `PasswordChangeView` в сочетании с `PasswordChangeForm` (или `UserPasswordChangeForm`). В этом сценарии пользователь должен ввести свой старый пароль для подтверждения личности, а затем дважды ввести новый пароль.

Таким образом, `PasswordResetConfirmView` и `PasswordChangeView` обеспечивают установку нового пароля, но для разных сценариев и с разными формами: `SetPasswordForm` используется без старого пароля, а `PasswordChangeForm` — со старым паролем.

### Маршрут для смены пароля `reset/done/` 🎉

Финальный этап процесса восстановления пароля — это страница подтверждения успешного сброса. После того как пользователь успешно установил новый пароль, он перенаправляется на этот маршрут, где ему отображается сообщение об успешном завершении операции. Это завершает цикл восстановления доступа.

Для этого этапа используется URL-путь `users/reset/done/`, шаблон `password_reset_complete.html` для отображения сообщения и класс `CustomPasswordResetCompleteView`, который наследуется от `PasswordResetCompleteView` и обрабатывает логику отображения этой страницы.

После того как мы рассмотрели все этапы восстановления пароля, перейдем к настройке почтового сервера, который позволит нашему приложению отправлять необходимые письма.

## Настройка `SMTP` сервера Яндекс ⚙️

В этом разделе мы подробно рассмотрим процесс настройки SMTP-сервера Яндекс для вашего Django-приложения. Правильная конфигурация почтового сервера является ключевым шагом для обеспечения функциональности восстановления пароля и отправки других уведомлений пользователям.

### Шаг 1: Настройка доступа к почтовому ящику на Яндексе 🔑

Для использования почтового ящика на Яндексе для отправки писем необходимо выполнить ряд настроек безопасности и доступа непосредственно в аккаунте Яндекс.Почты. Это включает в себя активацию доступа для почтовых программ и генерацию специального пароля для приложений.

Эти действия гарантируют, что ваше Django-приложение сможет безопасно аутентифицироваться на SMTP-сервере Яндекса без использования вашего основного пароля, что является лучшей практикой безопасности.

1. **Вход в почтовый ящик**:
   - Откройте [https://mail.yandex.ru/](https://mail.yandex.ru/) и войдите в ваш почтовый ящик.

2. **Доступ к настройкам**:
   - Перейдите в раздел "Настройки", который находится обычно в верхней панели.

3. **Почтовые программы**:
   - В разделе "Все настройки" найдите категорию "Почтовые программы".
   - Активируйте опции, связанные с доступом почтовых клиентов к почтовому ящику через IMAP, установив необходимые галочки.

4. **Генерация пароля для приложения**:
   - В разделе "Управление аккаунтом" перейдите в "Безопасность" -> "Доступы и пароли".
   - Выберите "Пароли приложений", выберите тип приложения "Почта", и следуйте инструкциям для создания пароля приложения. Этот пароль будет использоваться вместо вашего обычного пароля для доступа к SMTP.

Для наглядности, вот скриншоты, иллюстрирующие процесс настройки:

![[Pasted image 20250217204920.png]]
Раздел "Почтовые программы" для активации доступа почтовых клиентов.

После активации доступа необходимо сгенерировать пароль для приложения. На следующем изображении показан интерфейс генерации пароля:

![[Pasted image 20250619231343.png]]
Процесс генерации пароля для приложения в разделе "Безопасность". На последнем шаге потребуется выбрать тип приложения:

![[Pasted image 20250619231400.png]]
Выбор типа приложения "Почта" при создании пароля.

### Шаг 2: Настройка Django для использования SMTP Яндекса 🛠️

После того как вы настроили свой почтовый ящик на Яндексе и получили пароль для приложения, следующим шагом является конфигурирование вашего Django-проекта для использования этих настроек. Все необходимые параметры указываются в файле `settings.py`.

Правильная настройка `settings.py` критически важна для корректной работы отправки электронной почты. Убедитесь, что вы закомментировали или удалили предыдущие настройки `EMAIL_BACKEND`, если они были направлены на вывод в консоль, чтобы избежать конфликтов.

1. **Изменение настроек почтового сервера**:
   - В файле `settings.py` вашего Django проекта найдите и закомментируйте текущую настройку `EMAIL_BACKEND`, если она настроена на вывод в консоль:

```python
# EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
```

2. **Установка параметров SMTP**:
   - Укажите следующие настройки для использования Яндекс SMTP сервера:

```python
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.yandex.ru'
EMAIL_PORT = 465
EMAIL_HOST_USER = 'ваш_яндекс_почтовый_ящик'
EMAIL_HOST_PASSWORD = 'пароль_приложения'
EMAIL_USE_SSL = True
DEFAULT_FROM_EMAIL = 'адрес_отправителя'
SERVER_EMAIL = 'адрес_отправителя'
EMAIL_ADMIN = 'адрес_для_административных_уведомлений'
```

### Шаг 3: Тестирование отправки почты 🧪

После завершения всех настроек SMTP в Django, крайне важно убедиться, что отправка электронной почты работает корректно. Это можно сделать несколькими способами, включая использование Django shell для отправки тестового письма или ручное тестирование через веб-интерфейс приложения.

Тестирование поможет выявить любые проблемы с конфигурацией, такие как неверные учетные данные, неправильные порты или проблемы с SSL/TLS, и позволит оперативно их устранить.

После настройки параметров вы можете протестировать отправку почты с помощью Django shell:

```bash
python manage.py shell
```

В оболочке выполните следующие команды:

```python
from django.core.mail import send_mail

send_mail(
    'Тестовое сообщение',
    'Это тестовое сообщение, отправленное из Django через Яндекс SMTP.',
    'ваш_яндекс_почтовый_ящик',  # Используйте DEFAULT_FROM_EMAIL, если он установлен
    ['получатель@пример.com'],
    fail_silently=False,
)
```

Это отправит тестовое письмо на указанный адрес электронной почты, что позволит вам убедиться, что настройки SMTP сервера Яндекс работают корректно.

Или проведите ручное тестирование отправки почты через веб-интерфейс вашего приложения.
